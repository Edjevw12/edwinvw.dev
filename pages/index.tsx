import DarkModeButton from 'components/Button/DarkModeButton';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <DarkModeButton />
    </div>
  );
};

export default Home;
